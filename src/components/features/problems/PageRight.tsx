import { ScrollArea } from "@/components/ui/scroll-area";

const PageRight = () => {
  return (
    <ScrollArea className="w-full h-full max-h-[calc(100vh-57px)] p-4">
      <div className="flex flex-col gap-4">
        Right Side card to be implemented
      </div>
    </ScrollArea>
  );
};

export default PageRight;
